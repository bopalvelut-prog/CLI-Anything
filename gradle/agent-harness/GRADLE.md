# gradle

Gradle - build, test, dependencies

Binary: `gradle`
Category: build

## Commands

- `run [args...]` - Run arbitrary gradle command

## Usage

```bash
cli-anything-gradle run --help
cli-anything-gradle run [args...]
```
