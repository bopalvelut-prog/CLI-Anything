# GALAXY CLI Skill

## Description
Ansible Galaxy roles using the `ansible-galaxy` binary.

## Usage
```bash
python -m cli_anything.galaxy --help
python -m cli_anything.galaxy repl
```

## Commands
search query info install init list remove collection
