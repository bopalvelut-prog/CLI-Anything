# composer

PHP packages - install, require, update

Binary: `composer`
Category: packages

## Commands

- `run [args...]` - Run arbitrary composer command

## Usage

```bash
cli-anything-composer run --help
cli-anything-composer run [args...]
```
