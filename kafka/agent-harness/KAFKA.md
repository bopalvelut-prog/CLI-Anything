# kafka

Event streaming - topics, produce, consume

Binary: `kafka-topics`
Category: messaging

## Commands

- `run [args...]` - Run arbitrary kafka-topics command

## Usage

```bash
cli-anything-kafka run --help
cli-anything-kafka run [args...]
```
