# NVM CLI Skill

## Description
Node version manager using the `nvm` binary.

## Usage
```bash
python -m cli_anything.nvm --help
python -m cli_anything.nvm repl
```

## Commands
list remote install use uninstall current alias which
