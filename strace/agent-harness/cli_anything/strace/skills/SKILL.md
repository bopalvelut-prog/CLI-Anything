# STRACE CLI Skill

## Description
Trace execution syscalls using the `strace` binary.

## Usage
```bash
python -m cli_anything.strace --help
python -m cli_anything.strace repl
```

## Commands
trace binary follow filter output summary count
