# nfs

NFS exports - add, remove, mount

Binary: `exportfs`
Category: network

## Commands

- `run [args...]` - Run arbitrary exportfs command

## Usage

```bash
cli-anything-nfs run --help
cli-anything-nfs run [args...]
```
