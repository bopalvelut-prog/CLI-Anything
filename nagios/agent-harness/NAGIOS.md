# nagios

Monitoring - hosts, services, downtime

Binary: `nagios`
Category: monitoring

## Commands

- `run [args...]` - Run arbitrary nagios command

## Usage

```bash
cli-anything-nagios run --help
cli-anything-nagios run [args...]
```
