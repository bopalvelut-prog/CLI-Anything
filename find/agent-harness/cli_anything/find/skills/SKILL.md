# FIND CLI Skill

## Description
File search using the `find` binary.

## Usage
```bash
python -m cli_anything.find --help
python -m cli_anything.find repl
```

## Commands
search path name type size mtime exec user
