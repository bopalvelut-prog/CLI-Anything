# PS CLI Skill

## Description
Process status using the `ps` binary.

## Usage
```bash
python -m cli_anything.ps --help
python -m cli_anything.ps repl
```

## Commands
list user tree info kill name top
