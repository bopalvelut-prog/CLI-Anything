# zabbix

Monitoring - hosts, triggers, problems

Binary: `zabbix`
Category: monitoring

## Commands

- `run [args...]` - Run arbitrary zabbix command

## Usage

```bash
cli-anything-zabbix run --help
cli-anything-zabbix run [args...]
```
