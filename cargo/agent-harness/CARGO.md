# cargo

Rust - build, test, publish, dependencies

Binary: `cargo`
Category: build

## Commands

- `run [args...]` - Run arbitrary cargo command

## Usage

```bash
cli-anything-cargo run --help
cli-anything-cargo run [args...]
```
