# sentry

Error tracking - issues, releases, events

Binary: `sentry`
Category: monitoring

## Commands

- `run [args...]` - Run arbitrary sentry command

## Usage

```bash
cli-anything-sentry run --help
cli-anything-sentry run [args...]
```
