# swarm

Docker Swarm - services, nodes, scaling

Binary: `docker`
Category: containers

## Commands

- `run [args...]` - Run arbitrary docker command

## Usage

```bash
cli-anything-swarm run --help
cli-anything-swarm run [args...]
```
