# SED CLI Skill

## Description
Stream editor using the `sed` binary.

## Usage
```bash
python -m cli_anything.sed --help
python -m cli_anything.sed repl
```

## Commands
replace pattern replacement inplace delete line insert text extract
