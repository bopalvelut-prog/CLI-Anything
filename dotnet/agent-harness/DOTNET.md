# dotnet

.NET - build, run, test, publish

Binary: `dotnet`
Category: build

## Commands

- `run [args...]` - Run arbitrary dotnet command

## Usage

```bash
cli-anything-dotnet run --help
cli-anything-dotnet run [args...]
```
