# wireguard

WireGuard VPN - config, keys, peers

Binary: `wg`
Category: vpn

## Commands

- `run [args...]` - Run arbitrary wg command

## Usage

```bash
cli-anything-wireguard run --help
cli-anything-wireguard run [args...]
```
