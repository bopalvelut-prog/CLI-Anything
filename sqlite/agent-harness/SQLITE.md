# sqlite

SQLite query, import/export CSV, dump

Binary: `sqlite3`
Category: database

## Commands

- `run [args...]` - Run arbitrary sqlite3 command

## Usage

```bash
cli-anything-sqlite run --help
cli-anything-sqlite run [args...]
```
