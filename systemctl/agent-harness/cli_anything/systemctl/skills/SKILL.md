# SYSTEMCTL CLI Skill

## Description
Systemd service control using the `systemctl` binary.

## Usage
```bash
python -m cli_anything.systemctl --help
python -m cli_anything.systemctl repl
```

## Commands
status start stop restart enable disable list-units journal mask reload
