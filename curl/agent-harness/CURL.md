# curl

HTTP client - GET, POST, download, headers

Binary: `curl`
Category: http

## Commands

- `run [args...]` - Run arbitrary curl command

## Usage

```bash
cli-anything-curl run --help
cli-anything-curl run [args...]
```
