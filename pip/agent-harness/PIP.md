# pip

Python packages - install, list, freeze

Binary: `pip`
Category: packages

## Commands

- `run [args...]` - Run arbitrary pip command

## Usage

```bash
cli-anything-pip run --help
cli-anything-pip run [args...]
```
