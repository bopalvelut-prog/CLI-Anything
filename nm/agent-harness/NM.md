# nm

NetworkManager - devices, connections, WiFi

Binary: `nmcli`
Category: network

## Commands

- `run [args...]` - Run arbitrary nmcli command

## Usage

```bash
cli-anything-nm run --help
cli-anything-nm run [args...]
```
