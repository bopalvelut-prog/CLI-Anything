# packer

Machine image building - build, validate

Binary: `packer`
Category: infra

## Commands

- `run [args...]` - Run arbitrary packer command

## Usage

```bash
cli-anything-packer run --help
cli-anything-packer run [args...]
```
