# vagrant

VM lifecycle - init, up, halt, snapshot, SSH

Binary: `vagrant`
Category: vm

## Commands

- `run [args...]` - Run arbitrary vagrant command

## Usage

```bash
cli-anything-vagrant run --help
cli-anything-vagrant run [args...]
```
