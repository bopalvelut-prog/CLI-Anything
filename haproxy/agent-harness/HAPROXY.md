# haproxy

Load balancer - backends, servers, stats

Binary: `haproxy`
Category: web

## Commands

- `run [args...]` - Run arbitrary haproxy command

## Usage

```bash
cli-anything-haproxy run --help
cli-anything-haproxy run [args...]
```
