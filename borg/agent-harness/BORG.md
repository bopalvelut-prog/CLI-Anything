# borg

Deduplicated backup - create, list, prune

Binary: `borg`
Category: backup

## Commands

- `run [args...]` - Run arbitrary borg command

## Usage

```bash
cli-anything-borg run --help
cli-anything-borg run [args...]
```
