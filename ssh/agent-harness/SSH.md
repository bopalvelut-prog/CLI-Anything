# ssh

Remote access, key management, tunnels

Binary: `ssh`
Category: remote

## Commands

- `run [args...]` - Run arbitrary ssh command

## Usage

```bash
cli-anything-ssh run --help
cli-anything-ssh run [args...]
```
