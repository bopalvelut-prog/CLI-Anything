# wget

Download files, mirror sites, recursive fetch

Binary: `wget`
Category: download

## Commands

- `run [args...]` - Run arbitrary wget command

## Usage

```bash
cli-anything-wget run --help
cli-anything-wget run [args...]
```
