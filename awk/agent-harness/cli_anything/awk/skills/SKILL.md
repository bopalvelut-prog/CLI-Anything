# AWK CLI Skill

## Description
Text processing using the `awk` binary.

## Usage
```bash
python -m cli_anything.awk --help
python -m cli_anything.awk repl
```

## Commands
filter program field pattern action sum column count
