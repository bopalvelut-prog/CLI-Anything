# fail2ban

Intrusion prevention - ban, unban, jails

Binary: `fail2ban-client`
Category: security

## Commands

- `run [args...]` - Run arbitrary fail2ban-client command

## Usage

```bash
cli-anything-fail2ban run --help
cli-anything-fail2ban run [args...]
```
